import React from 'react';
import ReactDOM from 'react-DOM';

document.addEventListener('DOMContentLoaded', () => {
  const root = getElementById('root'); 
  ReactDOM.render(<h1>Hello Young World</h1>, root);
});